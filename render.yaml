services:
  - type: web
    name: museum-aceh-api
    runtime: node
    buildCommand: npm install
    startCommand: node src/app.js
    envVars:
      - key: NODE_ENV
        value: production